# OpenAI (required for embeddings: seed_knowledge_base.py)
OPENAI_API_KEY=sk-...

# Grok / xAI (optional — replaces OpenAI for the chat agent)
# Get key from: https://console.x.ai
# Set AGENT_MODEL and AGENT_BASE_URL to use Grok instead of GPT-4o
GROK_API_KEY=xai-...
AGENT_MODEL=grok-2-latest
AGENT_BASE_URL=https://api.x.ai/v1

# PostgreSQL — use DATABASE_URL for cloud (Neon/Supabase), or individual vars locally
DATABASE_URL=postgresql://user:password@ep-xxx.neon.tech/neondb?sslmode=require
# OR set individual vars for local dev:
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=fte_db
POSTGRES_USER=fte_user
POSTGRES_PASSWORD=changeme

# Kafka
KAFKA_BOOTSTRAP_SERVERS=localhost:9092

# Gmail
GMAIL_CREDENTIALS_PATH=./credentials/gmail-credentials.json
GMAIL_PUBSUB_TOPIC=projects/YOUR_GCP_PROJECT/topics/gmail-notifications

# Twilio (WhatsApp)
TWILIO_ACCOUNT_SID=ACxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_AUTH_TOKEN=xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
TWILIO_WHATSAPP_NUMBER=whatsapp:+14155238886

# Application
ENVIRONMENT=development
LOG_LEVEL=INFO
